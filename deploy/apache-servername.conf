# Silences Apache's "Could not reliably determine the server's fully qualified
# domain name" (AH00558) startup notice from the youtube-operational-api
# container. Mounted into /etc/apache2/conf-enabled/ via docker-compose.
ServerName localhost
